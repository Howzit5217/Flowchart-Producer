# Flowchart-Producer
Paste pseudocode, get a flowchart — style it (colors, fonts, sizes, bold, highlighting), run it and watch what it holds, send it as a link, export it as SVG, PNG or code. No install.
